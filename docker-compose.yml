version: '3.4'

services:
  jekyll:
    image: jekyll/jekyll:latest
    command: jekyll serve --watch --force_polling --incremental --verbose
    volumes:
      - .:/srv/jekyll
    ports:
      - 40000:4000